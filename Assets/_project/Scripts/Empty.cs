using UnityEngine;

namespace _project.Scripts
{
    public class Empty : MonoBehaviour
    {
        [SerializeField] private PhaseObjectTest phaseObjectTest;
        // Start is called before the first frame update
        void Start()
        {
           
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
